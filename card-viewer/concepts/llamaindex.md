---
slug: llamaindex
theme: LLM
domainLabel: LLM/Agent
subLabel: 대표 프레임워크 · 라이브러리
title: LlamaIndex: 데이터 연결과 RAG 파이프라인에 특화된 프레임워크
related: LangChain · LangGraph
---

## 도입
LlamaIndex의 핵심은 다양한 데이터 커넥터와 인덱스 구조다. 파일과 웹페이지와 SQL 데이터베이스와 여러 SaaS API에서 데이터를 읽어오는 로더를 제공하고 읽어온 문서를 청크로 나눈 뒤 벡터 인덱스나 트리 인덱스나 키워드 인덱스 같은 여러 인덱스 구조 중 하나로 조직화한다. 벡터 인덱스가 가장 널리 쓰이지만 문서 사이의 계층 구조가 중요한 경우 트리 인덱스처럼 다른 구조를 선택할 수 있다.

질의 시점에는 쿼리 엔진이 인덱스에서 관련 조각을 찾아 모델에 전달하고 응답을 합성한다. LlamaIndex는 이 검색과 합성 과정에 재순위화와 여러 인덱스를 동시에 조회하는 라우터와 하위 질문으로 쪼개 각각 검색한 뒤 합치는 기법처럼 RAG 품질을 높이기 위한 여러 모듈을 함께 제공한다.

에이전트 기능도 지원하지만 LlamaIndex의 강점은 여전히 데이터 계층에 있다. 에이전트가 판단 중에 필요한 정보를 검색해오는 도구로 쿼리 엔진을 등록해 쓰는 식으로 LangChain이나 LangGraph 같은 오케스트레이션 프레임워크와 함께 조합해 쓰는 경우도 흔하다.

## 명제



## 문제
벡터가 단위벡터이므로 코사인 유사도는 내적과 같다. $\bar v$는 $m$개의 하위 주제 벡터를 평균한 것이므로, $v_k$와의 내적 $\bar v\cdot v_k$는 $m$개의 항 $v_i\cdot v_k$를 모두 더한 뒤 $m$으로 나눈 값과 같다. 직교 가정에 의해 이 합에서 $i\neq k$인 항은 모두 $0$이고 $i=k$인 항만 $v_k\cdot v_k=1$로 남는다. 따라서 $\bar v\cdot v_k=$==빈칸== 이다.

## 해설
직교 가정으로 m개 항 중 i=k인 단 하나만 값 1로 남고 나머지는 모두 0이 되어 합이 1이 되는데, 애초에 이 합을 m으로 나눈 것이 평균이었으므로 1을 m으로 나눈 1/m이 최종값이 돼요.

**정답: $1/m$**

## 예시

