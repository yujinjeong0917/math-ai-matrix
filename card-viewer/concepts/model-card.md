---
slug: model-card
theme: MLOPS
domainLabel: MLOps · 인프라
subLabel: 레지스트리
title: 모델 카드: 성능과 한계, 의도된 쓰임새를 적어두는 설명서
related: 모델 버저닝 · 승인 워크플로우
---

## 도입
모델 카드에는 몇 가지 구획이 반복됩니다. 학습에 쓴 데이터와 그 출처 · 평가에 쓴 지표와 데이터셋별 성능 · 알려진 한계와 실패 사례 · 의도된 사용처와 의도되지 않은 사용처 그리고 공정성이나 편향 관련 점검 결과입니다. 성능 수치 하나만 덜렁 적어두는 대신 어떤 하위 집단에서 성능이 떨어지는지까지 나눠서 보여주는 것이 핵심입니다.

모델 카드가 없으면 모델을 가져다 쓰는 다른 팀은 전체 정확도 하나만 보고 판단하게 됩니다. 전체 정확도가 높아도 특정 언어권이나 특정 이미지 조건에서 성능이 크게 떨어지는 모델일 수 있는데 이런 정보가 문서화되어 있지 않으면 실제 서비스에 투입한 뒤에야 문제를 발견하게 됩니다.

모델 카드는 모델 버저닝과 짝을 이룹니다. 버전이 올라갈 때마다 카드도 함께 갱신되어야 카드가 실제 배포된 모델과 어긋나지 않습니다. 레지스트리에 모델을 등록할 때 카드 작성을 필수 항목으로 걸어두는 팀이 많은 이유입니다.

## 명제



## 문제
전체 정확도 $\text{Acc}_{\text{total}}$은 전체 맞힌 개수를 전체 표본수 $n_A+n_B$로 나눈 값이다. 두 그룹에서 맞힌 개수를 더해 표현하면 $\text{Acc}_{\text{total}}=$==빈칸== 이다.

## 해설
전체 맞힌 개수는 두 그룹의 맞힌 개수를 더한 $n_A\text{Acc}_A+n_B\text{Acc}_B$이고 전체 표본수는 $n_A+n_B$이므로, 전체 정확도는 이 둘을 나눈 값이에요. 결국 전체 정확도는 그룹별 정확도의 가중평균이 돼요.

**정답: $\dfrac{n_A\text{Acc}_A + n_B\text{Acc}_B}{n_A+n_B}$**

## 예시

