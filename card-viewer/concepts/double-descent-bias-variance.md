---
slug: double-descent-bias-variance
theme: PROB
domainLabel: 확률 · 통계
subLabel: 통계적 추론
title: 이중강하: 파라미터가 데이터보다 많아지는 순간 왜 테스트 오차가 다시 줄어드는가
hook: 편향-분산 트레이드오프는 모델의 복잡도를 하나의 축으로 놓고 그 위에서 테스트 오차가 U자 모양을 그린다고 말합니다.
related: 고전적 편향-분산 U자 분해 · 유한 기저로 함수를 근사하는 발상
---

## 기본설명
파라미터 수 $p$가 훈련 표본 수 $n$과 같아지는 보간임계점 근처에서 테스트 오차는 국소적인 최댓값을 갖는다. $p$가 $n$을 넘어 계속 커지면 테스트 오차는 다시 감소하는 경향을 보이는데, 이 두 번째 감소는 훈련오차가 0인 해가 여러 개 존재하는 영역에서 경사하강법이 그 중 노름이 작은 특정 해로 수렴하는 경향과 관련이 있다고 여겨진다. 다만 이 후자의 주장은 엄밀한 정리라기보다 부분적으로 경험적이고 여전히 활발히 연구되는 설명이다.

## 문제
지금 목표는 훈련오차가 정확히 0이 될 수 있는 최소한의 파라미터 수를 짚어내는 것이다. 훈련 데이터가 $n$개이고 각 데이터가 모델의 파라미터에 대해 하나의 선형 제약을 준다고 생각하면, $n$개의 점을 모두 정확히 지나는 함수를 찾는 문제는 미지수가 $p$개, 방정식이 $n$개인 연립방정식을 푸는 것과 같다. 미지수 수가 방정식 수보다 적으면 일반적으로 모든 방정식을 동시에 만족시킬 수 없어 훈련오차가 0보다 크게 남는다.

미지수 수가 방정식 수를 처음 따라잡는 지점을 보간임계점이라 부르고, 이 지점은 $p=$==빈칸== 에서 나타난다.

## 해설
방정식 수가 곧 훈련 표본 수 $n$이므로, 미지수인 파라미터 수 $p$가 $n$과 같아지는 순간이 정확히 해가 존재할 자유도와 제약의 개수가 균형을 이루는 경계다. 이 경계를 보간임계점이라 부른다.

**정답: $n$**

## 예시
파라미터 수와 데이터 수의 관계가 실제로 무엇을 바꾸는지 두 단계로 나누어 봅니다.

훈련 표본이 $n=10$개 있고 모델의 파라미터 수를 $p$로 바꿔가며 다항회귀를 적합한다고 하겠습니다. $p<10$인 구간에서는 데이터를 정확히 통과할 자유도가 부족해 편향이 남습니다. $p=10$이 되면 계수를 정하는 연립방정식의 계수행렬이 정칙이라는 전제 아래 10개의 점을 정확히 지나는 다항식이 정확히 하나로 정해집니다. 자유도가 $p-n=0$이라 이 유일한 해는 잡음이 조금만 바뀌어도 계수가 크게 요동칠 수 있고, 그래서 $p=10$ 부근에서 테스트 오차가 크게 튀어 오릅니다. $p>10$으로 더 늘리면 훈련오차가 0인 해들의 집합은 차원이 $p-10$인 아핀 부분공간을 이룹니다. 이제는 그 안에서 어떤 해를 고를지 선택의 여지가 생깁니다.

이 선택이 왜 도움이 되는지 아주 작은 예로 확인해봅니다. 데이터는 $x=1$에서 $y=1$ 하나뿐이고, 특성으로 $x,x^2$ 두 개를 쓰는 모델 $y=ax+bx^2$을 적합한다고 하겠습니다. 데이터를 정확히 지나려면 $a+b=1$만 만족하면 되므로 무한히 많은 $(a,b)$ 쌍이 훈련오차 0을 줍니다.

이 중 노름 $a^2+b^2$이 가장 작은 해를 라그랑주 승수법으로 구하면 $a=b=0.5$가 되어 최소노름은 $0.5^2+0.5^2=0.5$입니다. 반면 $a=2,\ b=-1$도 $2+(-1)=1$이라 훈련오차는 똑같이 0이지만 노름은 $2^2+(-1)^2=5$로 훨씬 큽니다.

참 함수가 $y=x$라서 $x=2$에서 참값이 $2$라 하겠습니다. 최소노름 해의 예측은 $0.5\times2+0.5\times4=1+2=3$이라 오차가 $|3-2|=1$입니다. 반면 노름이 큰 해의 예측은 $2\times2+(-1)\times4=4-4=0$이라 오차가 $|0-2|=2$로 더 큽니다.

이 작은 예는 증명이 아니라 관찰입니다. 하지만 훈련오차가 0인 해들 중에서도 노름이 작은 해가 이번 경우에는 더 나은 예측을 준 이 경향은, 경사하강법이 작은 초기값에서 출발했을 때 왜 노름이 작은 해 쪽으로 끌리는 경향을 보이는지에 대한 직관을 줍니다. 아래 절들은 이 직관을 좀 더 체계적으로 따라가 보되, 이중강하의 두 번째 하강 구간에 대한 설명이 여전히 정성적인 수준에 머문다는 점을 분명히 밝힙니다.
