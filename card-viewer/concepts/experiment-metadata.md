---
slug: experiment-metadata
theme: MLOPS
domainLabel: MLOps · 인프라
subLabel: 실험 기록
title: 실험 메타데이터: 데이터 버전과 코드 버전을 함께 기록하기
related: 모델 버저닝 · 데이터 품질 체크
---

## 도입
실험 하나를 완전히 재현하려면 최소한 세 가지가 필요합니다. 코드가 어느 커밋이었는지 학습에 쓴 데이터가 정확히 어느 스냅샷이었는지 그리고 하이퍼파라미터 값이 무엇이었는지입니다. 코드 버전은 깃 커밋 해시로 쉽게 남길 수 있지만 데이터 버전은 따로 챙기지 않으면 흔적이 사라집니다. 데이터셋은 계속 갱신되고 잘못된 라벨이 고쳐지고 새 행이 추가되기 때문에 같은 이름의 데이터셋이라도 시점마다 내용이 다릅니다.

실험 추적 도구는 실행 하나마다 코드 커밋 해시와 데이터 스냅샷 식별자와 하이퍼파라미터와 결과 지표를 한 레코드로 묶어 저장합니다. 이렇게 하면 실행 하나는 코드 버전과 데이터 버전과 하이퍼파라미터 조합으로 유일하게 식별되고 나중에 그 조합을 그대로 다시 실행하면 같은 결과를 재현할 수 있습니다.

이 기록이 없을 때 벌어지는 일이 더 와닿는 설명입니다. 지난달 리더보드 1등이었던 모델을 다시 학습시켰는데 성능이 떨어졌다면 원인이 코드 변경 때문인지 데이터가 바뀌었기 때문인지 구분할 방법이 없습니다. 데이터 버전을 코드 버전과 별도로 기록해두면 두 축을 각각 되돌려보면서 원인을 좁혀나갈 수 있습니다.

## 명제



## 문제
세 값을 모두 기록해서 각 실행을 코드, 데이터, 하이퍼파라미터의 삼중쌍으로 저장한다면, 서로 다르게 구별될 수 있는 실행 조합의 최대 가짓수는 곱의 법칙에 의해 ==빈칸== 이다.

## 해설
세 값이 서로 독립적으로 달라질 수 있으므로, 서로 다른 조합의 총 개수는 곱의 법칙에 따라 $n_c$, $n_d$, $n_h$를 모두 곱한 값이 되기 때문이에요.

**정답: $n_c \times n_d \times n_h$**

## 예시

