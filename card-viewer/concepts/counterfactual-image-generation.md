---
slug: counterfactual-image-generation
theme: XAI
domainLabel: XAI · 해석가능성
subLabel: 반사실 설명
title: 반사실 이미지 생성: 속성을 바꾼 가상의 이미지 만들기
related: Counterfactual 설명 · 재구성 충실도
---

## 도입
전형적인 절차는 이렇다. 먼저 오토인코더나 GAN으로 이미지를 잠재벡터 $z$로 인코딩한다. 그다음 분류기의 예측을 원하는 방향으로 바꾸면서 원본과 시각적으로 가까운 잠재벡터 $z'$를 최적화로 찾는다.

$$z' = \arg\min_{z'} \ \lambda \cdot \mathcal{L}_{\text{class}}(g(z'), y_{\text{target}}) + \lVert z - z' \rVert^2$$

$g$는 잠재벡터를 이미지로 복원하는 생성기다. 마지막으로 $g(z')$을 렌더링해 사람에게 보여준다. 생성기가 학습한 이미지 분포 안에서만 움직이기 때문에 표 데이터 counterfactual에서 문제가 됐던 존재할 법하지 않은 값 문제가 상당 부분 자동으로 해결된다.

StyleGAN 계열은 잠재공간의 특정 방향이 안경 나이 표정 같은 해석 가능한 속성과 대략적으로 대응된다는 점이 알려져 있다. 이 방향으로 $z$를 이동시키면 안경을 씌운 것과 같은 효과를 낼 수 있어 속성 단위 counterfactual을 만들기 쉽다. 다만 생성기 자체가 학습 데이터의 편향을 그대로 물려받기 때문에 반사실 이미지가 인종이나 성별과 얽힌 다른 속성까지 같이 바꿔버리는 얽힘 문제가 실무에서 자주 발생한다.

검증할 때는 생성된 반사실 이미지가 목표 속성 외에는 원본과 얼마나 비슷한지를 정체성 유사도로 확인하고 분류기가 실제로 예측을 뒤집었는지 별도로 확인한다.

## 명제



## 문제
여기서 $a = W^\top w$로 정의하면, $w^\top g(z') = w^\top W z'$는 ==빈칸==와 같다.

## 해설
g(z')=Wz'이고 a=W^\top w로 정의했으므로 w^\top W z' = (W^\top w)^\top z' = a^\top z'가 되기 때문이에요.

**정답: $a^\top z'$**

## 예시

